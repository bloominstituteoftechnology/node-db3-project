const db = require('../data/db-config.js');

function find() {

}

function findById(id) {
    
}

function findSteps(id) {
    
}

function add(schemeData) {
    
}

function remove(id) {
    
}

module.exports = {
    find,
    findById,
    findSteps,
    add,
    remove
}